//Sum of t wo numbers
#include <iostream>
using namespace std;
int main() {
    int a, b, sum;
    cout << "Enter first number: ";
    cin >> a;
    cout << "Enter second number: ";
    cin >> b;
    sum = a + b; // Using the addition operator to calculate the sum
    cout << "The sum of " << a << " and " << b << " is: " << sum << endl;
    return 0;
}